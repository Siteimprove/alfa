import { Property } from "./types";
import { Color, ColorProperty } from "./property/color";
import { FontSize, FontSizeProperty } from "./property/font";

export const Properties = {
  color: ColorProperty,
  fontSize: FontSizeProperty
};
