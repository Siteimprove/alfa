export function foo(foo: string): "foo" | "bar" {
  let b = 8;
  b = b + 1;
  if (foo === "bar") {
    let c = 8;
    c = c + 1 + 5 + ((1 * 1) / 4) * 4 * 54;
  }

  if (foo === "foo") {
    return "foo";
  }

  let a = 8;
  a = a + 1;

  return "bar";
}
