/**
 * @see https://drafts.csswg.org/css-shapes/#funcdef-path
 *
 * Path require parsing a SVG path data string, which we do not have currently.
 * Given our current use case of the CSS shapes (clipping elements to make them invisible),
 * it seems unlikely that a `path` will be used.
 *
 * So, we do not implement path until the need arise…
 */
