{
  "dedupe": {
    "command": "CI=false yarn dedupe",
    "description": "Deduplicate packages",
    "commitMsg": "Dedupe packages"
  },
  "depgraph": {
    "command": "yarn generate-dependency-graphs $(pwd) global",
    "description": "Update dependency graph",
    "commitMsg": "Update global dependency graph"
  },
  "extract": {
    "command": "CI=false yarn extract --quiet",
    "description": "Extract packages API",
    "commitMsg": "Extract API"
  },
  "packages-graphs": {
    "command": "yarn generate-dependency-graphs $(pwd) packages",
    "description": "Update individual packages dependency graphs",
    "commitMsg": "Update individual packages dependency graphs"
  }
}
