---
"@siteimprove/alfa-css": minor
---

**Breaking:** Values who include a color, such as `Shadow`, gradients, … now require a color resolver to be resolved (i.e. providing a value for `currentColor`).
