---
"@siteimprove/alfa-continuation": minor
---

**Breaking:** The `Continuation` namespace (and the `.of`, `.map` and `flatMap` functions) has been removed.

These functions were not used anywhere in the codebase.
