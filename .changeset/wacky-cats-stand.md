---
"@siteimprove/alfa-aria": minor
---

**Breaking:** The `DOM.hasIncorrectRoleWithoutName` predicate has been removed and a new `DOM.isNotLandmarkWithoutName` predicate replaces it, specifically for `<form>` elements.
