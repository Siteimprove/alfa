---
"@siteimprove/alfa-dom": patch
"@siteimprove/alfa-tree": patch
---

**Changed:** The `index()` method on nodes now accepts an optional predicate parameter to filter siblings when calculating the node's index position.
