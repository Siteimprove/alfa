---
"@siteimprove/alfa-selector": patch
---

**Added:** Selectors now have a `useContext` property telling whether the matching depends on context (`:focus`, `:hover`, …) or is purely structural (type or class selector, …)
