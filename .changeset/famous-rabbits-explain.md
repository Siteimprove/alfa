---
"@siteimprove/alfa-dom": patch
---

**Added:** A new function `getGroupedText` has been added to the `Query` namespace. The function takes a predicate and a label function that is used for grouping text nodes.
